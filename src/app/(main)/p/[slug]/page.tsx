import React from "react";

export default function Page({
  params,
}: {
  params: {
    slug: string;
  };
}) {
  //   find the product by this slug
  const slug = params.slug;
  return <div></div>;
}
